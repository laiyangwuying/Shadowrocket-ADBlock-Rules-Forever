# 构建时自动下载远端模块写入 factory/vendor/<路径>。
# 任一条下载失败时不覆盖本地已有同名文件。
#
# 格式（每行一条，制表符或多个空格皆可）：
#   <vendor 目录内相对路径>  <http(s) URL>
#
# 路径禁止含 .. ，仅允许写入 factory/vendor/ 之下。
#
# 专用模块以仓库 module/ 为准（已镜像 script-path 到 scripts/）。
# yfamilys.com 对 CI 返回 403，不再从此处拉取。
#
# ---- 按需取消注释或添加你的地址 ----
# AdBlock.module     https://yfamilys.com/module/AdBlock.module  # 由 factory/ad_module.py 构建
# YouTubeNoAd.sgmodule https://yfamilys.com/module/YouTubeNoAd.sgmodule
# xmlyad.module       https://yfamilys.com/module/xmlyad.module
# aqc.module          https://yfamilys.com/module/aqc.module
# caiyun.module       https://yfamilys.com/module/caiyun.module
# wechatad.module     https://yfamilys.com/module/wechatad.module
# adultra.module      https://yfamilys.com/module/adultra.module
