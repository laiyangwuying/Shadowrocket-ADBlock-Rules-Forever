[URL Rewrite]
# Redirect Google Search / Maps（.cn → .com）
^https?:\/\/(www\.)?google\.cn https://www.google.com 302
^https?:\/\/(ditu|maps)\.google\.cn https://maps.google.com 302
# YouTube 去广告请单独启用 module/YouTubeAd.sgmodule（勿与 AdBlock 重复注入）

# App 去广告（AdBlock.module 静态策略，见 factory/template/adblock_rewrite_static.txt）
{{adblock_rewrite_static}}
# EasyList China + AdGuard 中文 URL 规则（每日构建生成）
{{ad_url_rewrite}}

[MITM]
enable = true
h2 = true
hostname = {{adblock_mitm_hosts}}
