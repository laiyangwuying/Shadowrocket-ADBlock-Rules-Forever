---
description: 修正完成后自动提交并推送到 build（本仓库）
alwaysApply: true
---

# 修正后自动提交并推送

在本仓库完成 bug 修复、配置修正或构建相关改动后：

1. 若改动涉及 `factory/` 构建逻辑或广告/GFW 规则，先运行 `factory/build.py`（或 `./factory/auto_build.sh`）并确认通过
2. 运行 `git status` / `git diff` 确认变更范围
3. **自动创建 commit**（无需用户再说「提交」），message 简明说明修复原因
4. **自动推送到 `origin/build`**：`git pull --rebase origin build`（若落后）后 `git push origin build`
5. 使用环境变量设置作者（勿改 global git config）：
   - `GIT_AUTHOR_NAME` / `GIT_COMMITTER_NAME` = `laiyangwuying`
   - `GIT_AUTHOR_EMAIL` / `GIT_COMMITTER_EMAIL` = `laiyangwuying@163.com`

例外：用户明确说「先别提交/别推送」，或变更仅为未完成试验时，不提交、不推送。
